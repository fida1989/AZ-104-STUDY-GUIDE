
const list=document.getElementById('questionList');const search=document.getElementById('search');const filter=document.getElementById('domainFilter');
function render(){const term=(search.value||'').toLowerCase();const dom=filter.value;list.innerHTML='';QUESTIONS.filter(q=>{const hit=(q.question+' '+q.domain+' '+q.options.join(' ')+' '+q.explanation).toLowerCase().includes(term);return hit&&(dom==='all'||q.domain===dom)}).forEach(q=>{const el=document.createElement('article');el.className='q';el.innerHTML=`<small>${q.domain}</small><h3>${q.question}</h3>${q.options.map((o,i)=>`<button class='opt' data-i='${i}'>${o}</button>`).join('')}<div class='explain'><b>Answer:</b> ${q.options[q.answer]}<br>${q.explanation}</div>`;el.querySelectorAll('.opt').forEach(btn=>btn.onclick=()=>{el.classList.add('answered');el.querySelectorAll('.opt').forEach(b=>{b.disabled=true;if(+b.dataset.i===q.answer)b.classList.add('correct')});if(+btn.dataset.i!==q.answer)btn.classList.add('wrong')});list.appendChild(el)});if(!list.innerHTML)list.innerHTML='<div class="card">No questions found.</div>'}
search.addEventListener('input',render);filter.addEventListener('change',render);render();
document.querySelectorAll('.flash').forEach(f=>f.addEventListener('click',()=>f.classList.toggle('show')));
const topBtn=document.getElementById('topBtn');window.addEventListener('scroll',()=>{topBtn.style.display=scrollY>500?'block':'none';let current='';document.querySelectorAll('section,header').forEach(s=>{if(scrollY>=s.offsetTop-80)current=s.id});document.querySelectorAll('.sidebar a').forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+current))});topBtn.onclick=()=>scrollTo({top:0,behavior:'smooth'});
