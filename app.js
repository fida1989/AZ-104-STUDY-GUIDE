function renderQuestions(){const root=document.getElementById('questionList');const term=(document.getElementById('search').value||'').toLowerCase();root.innerHTML='';QUESTIONS.filter(q=>(q.domain+q.question+q.explanation+q.options.join(' ')).toLowerCase().includes(term)).forEach(q=>{const div=document.createElement('div');div.className='q';div.innerHTML=`<small>${q.domain}</small><h3>${q.question}</h3>${q.options.map((o,i)=>`<button class='opt' onclick='checkAnswer(this,${i},${q.answer})'>${String.fromCharCode(65+i)}. ${o}</button>`).join('')}<div class='explain'><b>Answer: ${String.fromCharCode(65+q.answer)}. ${q.options[q.answer]}</b><br>${q.explanation}</div>`;root.appendChild(div)})}function checkAnswer(btn,i,ans){const q=btn.closest('.q');q.classList.add('answered');q.querySelectorAll('.opt').forEach((b,idx)=>{b.disabled=true;if(idx===ans)b.classList.add('correct')});if(i!==ans)btn.classList.add('wrong')}renderQuestions();